# Architecture Decision Records

**ADR-001 through ADR-009 live in [`../ARCHITECTURE_ADDENDUM.md`](../ARCHITECTURE_ADDENDUM.md)
§2** (C#/.NET over Rust/C++, Avalonia, custom OpenGL renderer, no ECS, fixed timestep, JSON
source + compiled pack, template-exe export, rules in Core, Creator not owning game logic).
They are not duplicated here — the addendum is the source of truth for them.

New decisions (ADR-010+) are added here as individual append-only files:
`ADR-0NN-short-title.md`, each with **Decision / Context / Alternatives / Consequences /
Risks**. An ADR is superseded, never edited away; mark it `Superseded by ADR-0NN`.
