# Creature Sprite Sources

Create one lowercase `snake_case` directory per creature slug:

```text
creatures/<creature_slug>/sprite_sheet.png
```

Keep different creature forms or approved variants in clearly named files within the same creature
directory unless they have distinct stable entity IDs.

