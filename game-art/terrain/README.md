# Terrain Sprite Sources

Create one lowercase `snake_case` directory per terrain or tileset family:

```text
terrain/<terrain_slug>/sprite_sheet.png
```

Sheets should be reusable painting palettes or tile families rather than composed maps. Preserve
real PNG alpha, grid alignment, and meaningful non-duplicate variants.

